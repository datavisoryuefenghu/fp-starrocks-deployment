apiVersion: v1
name: fp-iceberg-storage
version: 1.0.0
appVersion: "1.0"
description: Iceberg storage pipeline for Feature Platform (FP Events → Kafka → Kafka Connect + SMT → S3 Parquet → Iceberg REST Catalog)
